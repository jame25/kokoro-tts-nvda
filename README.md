# kokoro-tts-nvda
A fast low-latency Text-to-Speech engine for NVDA using the Kokoro TTS model.

The `addon` directory contains an installable-layout NVDA 2026-compatible
driver and its automatic model and voice installer. See `addon/README.md` for
the runtime files that must be added when building a distributable package.
